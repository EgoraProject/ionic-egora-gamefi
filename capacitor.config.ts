import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.egora.gamefi',
  appName: 'ionic-egora-gamefi',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
